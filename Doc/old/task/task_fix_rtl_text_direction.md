# Task: رفع مشکل نمایش متن فارسی در صفحه پیش‌نمایش

**تاریخ:** 2025-10-26  
**وضعیت:** 🔄 در حال انجام

## شرح مشکل
در صفحه پیش‌نمایش داده‌ها (`data-importer/preview/`)، متن فارسی در بخش "نگاشت ستون‌ها" به صورت معکوس و از چپ به راست نمایش داده می‌شود. این مشکل به دلیل عدم تنظیم صحیح جهت متن (text-direction) برای عناصر جدول رخ می‌دهد.

## علت مشکل
- عدم تنظیم `dir="rtl"` برای سلول‌های جدول
- عدم استفاده از کلاس‌های CSS مناسب برای متن فارسی
- تنظیمات RTL فقط در سطح صفحه و نه در سطح عناصر داخلی

## راه‌حل پیشنهادی

### 1. به‌روزرسانی تمپلیت پیش‌نمایش
- افزودن `dir="rtl"` به سلول‌های جدول
- استفاده از کلاس‌های CSS برای اطمینان از نمایش صحیح فارسی
- تنظیم `text-align: right` برای سلول‌های حاوی متن فارسی

### 2. تست نمایش
- بررسی نمایش صحیح متن فارسی در جدول نگاشت ستون‌ها
- اطمینان از همترازی صحیح متن
- تست در مرورگرهای مختلف

## مراحل اجرا
1. بررسی و به‌روزرسانی تمپلیت `preview.html`
2. افزودن استایل‌های CSS لازم
3. تست نمایش در مرورگر
