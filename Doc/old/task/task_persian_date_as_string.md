# Task: تغییر فیلد تاریخ از Date به CharField برای پشتیبانی از تاریخ فارسی

**تاریخ:** 2025-10-26  
**وضعیت:** 🔄 در حال انجام

## شرح مشکل
سیستم با تاریخ‌های فارسی مشکل دارد و خطاهای مختلفی نمایش می‌دهد:
1. `مقدار «1402/01/01» در قالب نادرستی وارد شده است. تاریخ باید در قالب YYYY-MM-DD باشد.`
2. `مقدار تاریخ «1400-02-29» با اینکه در قالب درستی (YYYY-MM-DD) است ولی تاریخ ناممکنی را نشان می‌دهد.`

## راه‌حل پیشنهادی
- تغییر فیلد تاریخ از `DateField` یا `DateTimeField` به `CharField(max_length=10)`
- ذخیره تاریخ فارسی به صورت رشته 10 کاراکتری بدون هیچ تبدیل
- برخورد با تاریخ فارسی به عنوان رشته در تمام سیستم

## مراحل اجرا

### 1. تغییر مدل DocumentHeader
- تغییر فیلد `document_date` از `DateField` به `CharField(max_length=10)`
- به‌روزرسانی متدهای مرتبط

### 2. به‌روزرسانی سرویس یکپارچه‌سازی
- حذف توابع تبدیل تاریخ
- ذخیره مستقیم تاریخ فارسی به عنوان رشته

### 3. به‌روزرسانی ویوها و تمپلیت‌ها
- نمایش تاریخ فارسی بدون تغییر
- عدم نیاز به اعتبارسنجی فرمت تاریخ

### 4. ایجاد مایگریشن
- ایجاد مایگریشن برای تغییر نوع فیلد در دیتابیس

## مزایا
- حذف کامل مشکلات مربوط به تاریخ فارسی
- سادگی در ذخیره‌سازی و بازیابی
- عدم نیاز به تبدیل تاریخ
- سازگاری کامل با سیستم‌های مالی ایرانی
