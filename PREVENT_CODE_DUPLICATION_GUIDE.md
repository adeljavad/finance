# راهنمای جلوگیری از تکرار کد در Cline

## 🎯 مشکل: تکرار کدهای قبلی
شما نگران هستید که وقتی روی "تسک جدید" کلیک می‌کنید، من کدهایی که قبلاً نوشته‌اید را دوباره بنویسم.

## 🔧 راهکارهای عملی

### راهکار 1: استفاده از پرامپت‌های استاندارد

**پرامپت پیشنهادی برای شروع هر تسک جدید:**
```
قبل از شروع این تسک، لطفاً فایل‌های context زیر را کامل بخوان:
- PROJECT_CONTEXT.md (وضعیت فعلی پروژه)
- PROJECT_SPECIFIC_CONTEXT.md (contextهای خاص)
- USER_CONTEXT.md (ترجیحات من)

سپس فایل‌های مربوطه در پروژه را بررسی کن تا مطمئن شوی کد تکراری نمی‌نویسی.
```

**پرامپت برای تسک‌های توسعه:**
```
من می‌خواهم [توضیح تسک]. 
قبل از شروع:
1. PROJECT_CONTEXT.md را بخوان تا وضعیت پروژه را بدانی
2. فایل‌های [نام فایل‌های مربوطه] را بررسی کن
3. مطمئن شو کد تکراری نمی‌نویسی
4. از ساختار موجود پیروی کن
```

### راهکار 2: تنظیمات Cline/Sline

**در تنظیمات Cline/Sline این موارد را اضافه کنید:**

```json
{
  "default_instructions": "همیشه قبل از شروع هر task فایل‌های PROJECT_CONTEXT.md، USER_CONTEXT.md و PROJECT_SPECIFIC_CONTEXT.md را کامل بخوان. بررسی کن که کد تکراری ننویسی و از ساختار موجود پروژه پیروی کنی."
}
```

### راهکار 3: ساختار پروژه هوشمند

**فایل `AUTO_READ_CONTEXT.py` ایجاد کنید:**
```python
"""
این فایل به Cline یادآوری می‌کند که قبل از هر task فایل‌های context را بخواند.
"""

CONTEXT_FILES_TO_READ = [
    "PROJECT_CONTEXT.md",
    "USER_CONTEXT.md", 
    "PROJECT_SPECIFIC_CONTEXT.md",
    "QUICK_START_CONTEXT_GUIDE.md"
]

def remind_to_read_context():
    """
    همیشه قبل از شروع task جدید این تابع را صدا بزن
    تا مطمئن شوی contextها خوانده شده‌اند.
    """
    print("📖 یادآوری: قبل از شروع task، فایل‌های context را بخوان!")
    for file in CONTEXT_FILES_TO_READ:
        print(f"   - {file}")
```

### راهکار 4: پرامپت‌های سریع

**پرامپت‌های از پیش تعریف شده:**

**برای تسک‌های جدید:**
```
[تسک جدید] - [توضیح تسک]
دستور: قبل از شروع PROJECT_CONTEXT.md را بخوان
```

**برای توسعه ویژگی‌ها:**
```
[توسعه ویژگی] - [نام ویژگی]
دستور: contextها را بخوان و از کدهای موجود استفاده کن
```

**برای دیباگ:**
```
[دیباگ] - [مشکل]
دستور: contextها را بخوان و فایل‌های مربوطه را بررسی کن
```

## 🚀 workflow پیشنهادی

### مرحله 1: قبل از کلیک روی "تسک جدید"
1. مطمئن شوید فایل‌های context به‌روز هستند
2. پرامپت مناسب را آماده کنید

### مرحله 2: هنگام کلیک روی "تسک جدید"
از این پرامپت استفاده کنید:
```
من می‌خواهم [توضیح دقیق تسک] را انجام دهم.

دستورالعمل‌های ضروری:
1. قبل از هر کاری فایل‌های PROJECT_CONTEXT.md، USER_CONTEXT.md و PROJECT_SPECIFIC_CONTEXT.md را کامل بخوان
2. فایل‌های [لیست فایل‌های مربوطه] را بررسی کن
3. مطمئن شو کدی که می‌نویسی تکراری نیست
4. از ساختار و استانداردهای موجود پروژه پیروی کن
5. اگر شک داری، قبل از نوشتن کد جدید از من بپرس
```

### مرحله 3: پس از اتمام تسک
1. اگر کد جدید مهمی نوشته‌اید، آن را به فایل‌های context اضافه کنید
2. تاریخ به‌روزرسانی را ثبت کنید

## 📝 نمونه‌های عملی

### نمونه 1: توسعه ویژگی جدید
```
من می‌خواهم سیستم گزارش‌گیری مالی را توسعه دهم.

دستورالعمل:
1. PROJECT_CONTEXT.md را بخوان تا وضعیت پروژه را بدانی
2. فایل‌های financial_system/views.py و financial_system/models.py را بررسی کن
3. مطمئن شو گزارش‌گیری تکراری توسعه نمی‌دهی
4. از استانداردهای موجود پروژه پیروی کن
```

### نمونه 2: رفع خطا
```
من خطای ImportError در data_importer دارم.

دستورالعمل:  
1. contextها را بخوان
2. فایل data_importer/views.py و requirements.txt را بررسی کن
3. فقط چیزی که لازم است را تغییر بده
4. کدهای کارکرده را دست نزن
```

## 🛡️ تضمین‌ها

### کاری که من خواهم کرد:
- ✅ همیشه قبل از task فایل‌های context را می‌خوانم
- ✅ فایل‌های مربوطه را بررسی می‌کنم
- ✅ از کد تکراری جلوگیری می‌کنم
- ✅ از ساختار موجود پیروی می‌کنم

### کاری که شما باید انجام دهید:
- ✅ فایل‌های context را به‌روز نگه دارید
- ✅ از پرامپت‌های استاندارد استفاده کنید
- ✅ وقتی تسک جدید می‌زنید، فایل‌های مربوطه را مشخص کنید

---
**نتیجه**: با این راهکارها، شما می‌توانید با اطمینان روی "تسک جدید" کلیک کنید بدون نگرانی از تکرار کد!
